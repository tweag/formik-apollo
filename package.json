{
  "version": "1.4.0",
  "name": "formik-apollo",
  "repository": "https://github.com/promptworks/react-forms/tree/master/packages/formik-apollo",
  "description": "A React hook to extract validation errors from a GraphQL request and apply them to a Formik form.",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "bili --format cjs --format esm src/index.ts",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "apollo-client": "*",
    "formik": "^2.0",
    "graphql": "*",
    "react": ">=16.8"
  }
}
